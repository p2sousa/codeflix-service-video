language: php

php:
  - 7.3

before_script:
  - cp .docker/php/.env.travis .env.testing
  - mysql -e 'create database codeflix_videos_test;'
  - composer self-update
  - composer install --no-interaction

script:
  - vendor/bin/phpunit
